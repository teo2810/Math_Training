# Variante a schemi

File: `schema.html`

L'app classica resta su `index.html`.
Questa variante allena il riconoscimento dello schema (unire, cambiare, confrontare, gruppi uguali) prima del calcolo.

Dati in localStorage con prefisso `s_` — non si mischiano con la versione classica (`m_`).
